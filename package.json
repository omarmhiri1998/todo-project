{
  "name": "todo-project",
  "version": "1.0.0",
  "scripts": {
    "backend": "node backend/server.js",
    "frontend": "npm --prefix frontend/my-app run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.4.0",
    "concurrently": "^9.0.0"
  }
}
